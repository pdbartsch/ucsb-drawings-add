version: '3.8'

services:
  ucsb-drawings-gui:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/tmp
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
